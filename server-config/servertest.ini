# Server .ini overrides
#
# Reading speed (minutes per page). Default is 1.0.
# In MP you can't fast-forward, so 1 min/page means a long wait.
# 0.1 = ~6 seconds per page — fast enough to not be painful.
FastReadMultiplier=0.1
